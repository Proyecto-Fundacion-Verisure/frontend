VITE_API_URL=http://localhost:8080/api
VITE_APP_ORIGIN=http://localhost:5173

# Mocks · src/api/mocks.js. Toda la aplicación va contra el backend real; solo
# siguen en mock las dos pantallas sin controlador: ORG (/org/dashboard) y
# PROPOSAL (el formulario público de la landing). Están encendidos por defecto
# en desarrollo; VITE_USE_MOCKS=false los apaga y VITE_USE_<MODULO>_MOCKS manda
# sobre la global.
